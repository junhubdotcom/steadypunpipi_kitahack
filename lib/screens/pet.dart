import 'package:flutter/material.dart';

class PetPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Pet Page", style: TextStyle(fontSize: 20))),
    );
  }
}
